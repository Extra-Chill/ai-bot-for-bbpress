# AI Bot for bbPress Migration Plan (Archived)

This file is kept for historical context only.

The plugin has migrated to WordPress core Abilities + `wordpress/wp-ai-client` and no longer uses a bundled legacy AI client.

For the active migration notes, see `MIGRATION-WP-AI-CLIENT.md`.